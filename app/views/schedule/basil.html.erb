<div class="schedule">
  <h1><%= @selected_vegetable_name %>のスケジュール</h1>

  <% if @schedules.present? %>
    <% @schedules.each do |schedule| %>
      <div class="schedule-details">
        <%= schedule.details %>
      </div>
    <% end %>
  <% else %>
    <p>スケジュールはまだ登録されていません。</p>
  <% end %>

  <!-- ここに選択された野菜に特有の情報やスタイルを追加することができます -->
</div>
