<h1>バジルのスケジュール</h1>

<% @events.each do |event| %>
  <div>
    <h2><%= event.name %></h2>
    <p><%= event.start_date %> から <%= event.end_date %> まで</p>
    <p><%= event.description %></p>
    <%= link_to 'アドバイスを見る', advice_event_path(event) %>
  </div>
<% end %>
